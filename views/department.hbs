<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Department List</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f9;
            color: #333;
        }

        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 15px 30px;
            background: linear-gradient(135deg,#1c1b1b,#716c6c);
            

            color: #fff;
        }

        ul {
            list-style: none;
            display: flex;
            gap: 20px;
            margin: 0;
        }

        ul li a {
            text-decoration: none;
            color: #fff;
            font-size: 1em;
        }

        .logo {
            font-size: 1.5em;
            font-weight: bold;
        }

        a:hover {
            color: #16a085;
        }

        .container {
            display: flex;
            margin: 20px;
             margin-top: 30px;
        }

        .leftflex {
            display: flex; 
            flex-direction: column;
            position: relative;
            align-items: flex-start; /* Changed to flex-start for better alignment */
            width: 150px; /* Increased width for better visibility */
            height: auto; /* Changed to auto for flexible height */
            padding: 20px;
            box-shadow: 10px 0 15px rgba(0, 0, 0, 0.2); 
            background-color: #fff; /* Added background color for contrast */
            margin-bottom: 50px;
            min-height: 220px;
        }

        .lfa {
            font-size: 14px; /* Increased font size for better readability */
            padding: 10px; /* Increased padding for better click area */
            border-radius: 4px; /* Added border radius for better styling */
            transition: background-color 0.3s ease, color 0.3s ease;
            color: #575050;
            text-decoration: none;
        }

        .lfa:hover {
            background-color: #e7f2f2; /* Added hover effect */
        }

        hr {
            border: none;
            border-top: 2px solid #e0e0e0;
            margin: 20px 0;
        }

        footer {
            background-color: #333;
            color: #fff;
            text-align: center;
            padding: 15px 0;
            margin-top: 30px;
            font-size: 0.9em;
        }

        .profile-details {
            display: grid;
            grid-template-columns: repeat(3, 1fr); /* Simplified grid definition */
            gap: 25px;
            margin-left: 80px; 
            margin-bottom: 50px;
        }

        .profile-details div {
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: flex-start;
    background-color: #fff;
    height: 100px;
    padding: 15px;
    border-radius: 8px;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    transition: transform 0.2s ease, box-shadow 0.2s ease;
}

.profile-details div:hover {
    transform: translateY(-4px);
    box-shadow: 0 8px 12px rgba(0, 0, 0, 0.15);
}

.profile-details span {
    font-weight: bold;
    font-size: 0.9em;
    color: #2980b9;
    margin-bottom: 8px;
}

.profile-details p {
    margin: 0;
    font-size: 1em;
    color: #34495e;
}
.profile-details a{
             color: #575050;
            text-decoration: none;
            padding: 5px;;
}

.profile-details a:hover {
            background-color: #e7f2f2; 
            border-radius: 10px;
        }

.profile-details div:hover span, 
.profile-details div:hover p {
    color: #16a085;
}



        .profile-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            background-color: #2c3e50;
            color: #ffffff;
            padding: 15px;
            border-radius: 12px 12px 0 0;
            margin: 0 20px; /* Adjusted margin */
        }

        .profile-header h2 {
            margin: 0;
        }



.add-btn {
            margin-top: 20px;
            padding: 8px 15px;
            background-color: #686262;
            color: white;
            text-align: center;
            border-radius: 4px;
            font-weight: bold;
            cursor: pointer;
            text-decoration: none;
            transition: background-color 0.2s;
        }

        .add-btn:hover {
            background-color: #7e7575;
            color: white;
        }

    </style>
</head>
<body>
    <header>
        <h1 class="logo">SCMS</h1>
        <ul>
            <li><a href="/">HOME</a></li>
            <li><a href="/empd">Employee Section</a></li>
            <li><a href="/department">Department</a></li>
            <li><a href="/project">Project</a></li>
            <li><a href="#">About</a></li>
        </ul>
    </header>

    <div class="container">
        <div class="leftflex">
            {{#each rows}}
                <a class="lfa"  href="/departmentview/{{this.Dept_ID}}">{{this.Dept_ID}} - {{this.D_Name}}</a>
            {{/each}}
            <a href="/add-department" class="add-btn">+ New</a>
          
        </div>

        <div class="profile-details">
            {{#if row.length}}
                <div>
                    <span>Department ID:</span>
                    <p>{{row.[0].Dept_ID}}</p>
                </div>
                <div>
                    <span>Department Name:</span>
                    <p>{{row.[0].D_Name}}</p>
                </div>
                <div>
                    <span>Location:</span>
                    <p>{{row.[0].D_location}}</p>
                </div>
                <div>
                    <span>Manager:</span>
                    <a href="/view/{{row.[0].EID}}"><p>{{row.[0].E_Name}}</p></a>
                </div>
                <div>
                    <span>Employee Count:</span>
                    <p>{{row.[0].No_of_emp}}</p>
                </div>
                <div>
                    <a href="/achievements/{{row.[0].Dept_ID}}">Cick to View more</a>
                </div>
            {{else}}
                <p>Please select department to veiw details.</p>
            {{/if}}
        </div>
    </div>
    <footer>
        <p>&copy; 2024 SCMS. All rights reserved.</p>
    </footer>
</body>
</html>